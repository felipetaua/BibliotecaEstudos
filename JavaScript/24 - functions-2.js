/*
    Functions

    Arguments and Parameters

*/

function sum(number1, number2) {
    console.log(number1 * number2)
}

// console.log(number1) // os parametros só existem dentro da função

sum(2,3) // invoca a função, chama a função, invoke or call
sum(5, 8)